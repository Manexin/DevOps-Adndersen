--- 
- name: Test connection to my servers 
  hosts: all
  become: yes 

  tasks: 

   - name: Ping my servers
     ping:

   - name: Ping shell
     shell: ping -c 4 {{ ansible_host }}
     register: output 
     debug: var=output.stdout
